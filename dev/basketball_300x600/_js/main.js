import { init, start, colors } from "../../_common/js/basketball.js";

const barOptions = {
  TOTAL: 12,
  WIDTH: 276,
  HEIGHT: 14,
  GAP: 20,
  scale: { scale: 0.8, x: -142, y: -45 },
  ignoreText2: true,
};

start(barOptions);

module.exports = {};
