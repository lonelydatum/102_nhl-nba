import { init, start, colors } from "../../_common/js/basketball.js";

const barOptions = {
  TOTAL: 12,
  WIDTH: 276,
  HEIGHT: 14,
  GAP: 20,
  scale: { scale: 0.7, y: "+=25" },
};

start(barOptions);

module.exports = {};
