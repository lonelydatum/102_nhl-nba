import { init, start, colors } from "../../_common/js/basketball.js";

const barOptions = {
  TOTAL: 12,
  WIDTH: 132,
  HEIGHT: 15,
  GAP: 10,
  hero: 0.9,
  scale: { scale: 0.62, x: "+=5" },
};

start(barOptions);

module.exports = {};
